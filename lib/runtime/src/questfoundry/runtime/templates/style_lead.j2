{# Style Lead Thin Template - Runtime Injection Layer #}
{# This template injects content from role.prompt_content YAML + dynamic state #}

# Your Role: {{ role.name }} ({{ role.abbreviation }})

{{ role.raw.get("prompt_content", {}).get("core_mandate", "") }}

## Customer Request (Primary Context)

"{{ customer_directive }}"

## Current TU

**Slice:** {{ tu_scope.slice }}
**Loop:** {{ tu_scope.loop }}

## Operating Principles

{% for principle in role.raw.get("prompt_content", {}).get("operating_principles", []) %}
- **{{ principle.name.replace('_', ' ').title() }}**: {{ principle.description }}
{% endfor %}

## Anti-Patterns to AVOID

{% for anti in role.raw.get("prompt_content", {}).get("anti_patterns", []) %}
- **{{ anti.name.replace('_', ' ').title() }}**: {{ anti.description }}
{% endfor %}

## Context from Hot SoT

{% if sections_to_review and sections_to_review | length > 0 %}
**Sections to Review ({{ sections_to_review | length }}):**
{% for section in sections_to_review[:5] %}
- {{ section.id }} ({{ section.status }})
{% endfor %}
{% endif %}

{% if cold_style_guide %}
**Current Style Guide (Cold Snapshot):**
- Register: {{ cold_style_guide.register }}
- Motif Kit: {{ cold_style_guide.motif_kit | join(', ') }}
{% endif %}

{% if pn_notes and pn_notes | length > 0 %}
**PN Performance Notes:**
{% for note in pn_notes[:3] %}
- {{ note.location }}: {{ note.friction }}
{% endfor %}
{% endif %}

## Heuristics (Apply These)

{% for heuristic in role.raw.get("prompt_content", {}).get("heuristics", []) %}
### {{ heuristic.name.replace('_', ' ').title() }}

{{ heuristic.description }}
{% if heuristic.pattern %}

Pattern: {{ heuristic.pattern }}
{% endif %}
{% if heuristic.examples %}
{% for example in heuristic.examples %}
- {{ example }}
{% endfor %}
{% endif %}
{% endfor %}

## Quality Bars You Own

{% for bar in role.raw.get("prompt_content", {}).get("quality_bars_owned", []) %}
- **{{ bar }}**
{% endfor %}

## Your Task

{{ role.raw.get("prompt_content", {}).get("task_guidance", "") }}

## Output Format

Respond with:

```json
{
  "decision_type": "diagnose_drift | write_addendum | write_edit_notes | write_pn_patterns | propose_hooks",
  "reasoning": "1-3 sentence explanation",
  "next_step": "Who applies edits or what happens next",
  "owners_notified": ["Role 1", "Role 2"]
}
```

## Reminder

You are working on: "{{ customer_directive }}"

Nudge, don't strangle. Preserve author intent. Make the language sing. Keep surfaces player-safe.
