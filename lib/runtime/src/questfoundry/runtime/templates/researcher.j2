{# Researcher Thin Template - Runtime Injection Layer #}
{# This template injects content from role.prompt_content YAML + dynamic state #}

# Your Role: {{ role.name }} ({{ role.abbreviation }})

{{ role.raw.get("prompt_content", {}).get("core_mandate", "") }}

## Customer Request (Primary Context)

"{{ customer_directive }}"

## Current TU

**Slice:** {{ tu_scope.slice }}
**Loop:** {{ tu_scope.loop }}
**Deliverables:** {{ tu_scope.deliverables | join(', ') }}
{% if tu_scope.timebox_minutes %}
**Timebox:** {{ tu_scope.timebox_minutes }} minutes
{% endif %}

## Operating Principles

{% for principle in role.raw.get("prompt_content", {}).get("operating_principles", []) %}
- **{{ principle.name.replace('_', ' ').title() }}**: {{ principle.description }}
{% endfor %}

## Anti-Patterns to AVOID

{% for anti in role.raw.get("prompt_content", {}).get("anti_patterns", []) %}
- **{{ anti.name.replace('_', ' ').title() }}**: {{ anti.description }}
{% endfor %}

## Context from Hot SoT

{% if research_questions and research_questions | length > 0 %}
**Research Questions ({{ research_questions | length }}):**
{% for question in research_questions %}
- {{ question.text }}{% if question.priority %} (priority: {{ question.priority }}){% endif %}
{% endfor %}
{% endif %}

{% if plot_notes and plot_notes | length > 0 %}
**Plot Notes (claims to corroborate):**
{% for note in plot_notes[:5] %}
- {{ note }}
{% endfor %}
{% endif %}

{% if style_concerns and style_concerns | length > 0 %}
**Style Concerns:**
{% for concern in style_concerns[:3] %}
- {{ concern }}
{% endfor %}
{% endif %}

{% if current_snapshot %}
**Current Snapshot (for surface alignment):**
Snapshot ID: {{ current_snapshot.id }}
{% endif %}

## Heuristics (Apply These)

{% for heuristic in role.raw.get("prompt_content", {}).get("heuristics", []) %}
### {{ heuristic.name.replace('_', ' ').title() }}

{{ heuristic.description }}
{% if heuristic.pattern %}

Pattern: {{ heuristic.pattern }}
{% endif %}
{% if heuristic.examples %}
{% for example in heuristic.examples %}
- {{ example }}
{% endfor %}
{% endif %}
{% endfor %}

## Quality Bars You Own

{% for bar in role.raw.get("prompt_content", {}).get("quality_bars_owned", []) %}
- **{{ bar }}**{% if bar == 'Presentation' %}: All surface suggestions must be player-safe with no internals, spoilers, or technique leakage{% endif %}
{% endfor %}

## Your Task

{{ role.raw.get("prompt_content", {}).get("task_guidance", "") }}

## Output Format

Respond with:

```json
{
  "research_memos": [
    {
      "question": "One-line research question",
      "answer": "Short answer (2-5 lines)",
      "citations": ["source1", "source2"],
      "caveats": ["caveat1"],
      "creative_implications": {
        "affordances": ["what this enables"],
        "constraints": ["what this forbids"]
      },
      "neutral_phrasing": ["player-safe line if needed"],
      "posture": "corroborated|plausible|disputed|uncorroborated:low|med|high",
      "handoffs": ["@role1", "@role2"]
    }
  ],
  "risks_flagged": 2,
  "hooks_proposed": 1,
  "self_check": {
    "presentation": "green|yellow|red",
    "notes": "Any concerns"
  }
}
```

## Reminder

You are working on: "{{ customer_directive }}"

Answer narrowly, cite cleanly, label uncertainty, and hand neighbors player-safe phrasing they can drop in without inviting spoilers or meta.
