# Pre-commit hooks for QuestFoundry mono-repo
# See https://pre-commit.com for more information

repos:
  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        exclude: ^(spec/|lib/python/src/questfoundry/resources/)
      - id: end-of-file-fixer
        exclude: ^(spec/|lib/python/src/questfoundry/resources/)
      - id: check-yaml
      - id: check-json
      - id: check-toml
      - id: check-added-large-files
        args: ["--maxkb=1000"]
      - id: check-merge-conflict
      - id: check-case-conflict
      - id: mixed-line-ending

  # Python: Ruff (linting and formatting) - lib/compiler
  - repo: local
    hooks:
      - id: ruff-lib-compiler
        name: ruff (lib/compiler)
        entry: ruff check --fix --no-cache
        language: python
        types_or: [python, pyi]
        files: ^lib/compiler/
        additional_dependencies:
          - ruff==0.6.9
      - id: ruff-format-lib-compiler
        name: ruff format (lib/compiler)
        entry: ruff format --no-cache
        language: python
        types_or: [python, pyi]
        files: ^lib/compiler/
        additional_dependencies:
          - ruff==0.6.9

  # Python: Ruff (linting and formatting) - cli/prompt_generator
  - repo: local
    hooks:
      - id: ruff-cli-prompt-generator
        name: ruff (cli/prompt_generator)
        entry: ruff check --fix --no-cache
        language: python
        types_or: [python, pyi]
        files: ^cli/prompt_generator/
        additional_dependencies:
          - ruff==0.6.9
      - id: ruff-format-cli-prompt-generator
        name: ruff format (cli/prompt_generator)
        entry: ruff format --no-cache
        language: python
        types_or: [python, pyi]
        files: ^cli/prompt_generator/
        additional_dependencies:
          - ruff==0.6.9

  # Python: Ruff (linting and formatting) - lib/python
  # --no-cache has been disabled due to a locking problem, probably from the check and format hook fighting.
  - repo: local
    hooks:
      - id: ruff-lib-python
        name: ruff (lib/python)
        entry: ruff check --fix --no-cache
        language: python
        types_or: [python, pyi]
        files: ^lib/python/
        additional_dependencies:
          - ruff==0.6.9
      - id: ruff-format-lib-python
        name: ruff format (lib/python)
        entry: ruff format --no-cache
        language: python
        types_or: [python, pyi]
        files: ^lib/python/
        additional_dependencies:
          - ruff==0.6.9

  # Python: Type checking with mypy - lib/compiler
  - repo: local
    hooks:
      - id: mypy-lib-compiler
        name: mypy (lib/compiler)
        entry: mypy
        language: python
        types_or: [python, pyi]
        files: ^lib/compiler/src/
        args: [--config-file=lib/compiler/pyproject.toml]
        additional_dependencies:
          - mypy==1.11.2

  # Python: Type checking with mypy - cli/prompt_generator
  - repo: local
    hooks:
      - id: mypy-cli-prompt-generator
        name: mypy (cli/prompt_generator)
        entry: mypy
        language: python
        types_or: [python, pyi]
        files: ^cli/prompt_generator/src/
        args: [--config-file=cli/prompt_generator/pyproject.toml]
        additional_dependencies:
          - mypy==1.11.2
          - typer>=0.9.0
          - rich>=13.0.0
          - questionary>=2.0.0
          - pyyaml>=6.0

  # Python: Type checking with mypy - lib/python
  - repo: local
    hooks:
      - id: mypy-lib-python
        name: mypy (lib/python)
        entry: mypy
        language: python
        types_or: [python, pyi]
        files: ^lib/python/src/
        args: [--config-file=lib/python/pyproject.toml]
        additional_dependencies:
          - mypy==1.11.2

  # Markdown linting
  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.39.0
    hooks:
      - id: markdownlint
        args: [--fix]
        exclude: ^(lib/python/src/questfoundry/resources/|lib/python/docs/)

  # Commitizen for commit message format
  - repo: https://github.com/commitizen-tools/commitizen
    rev: v3.13.0
    hooks:
      - id: commitizen
        stages: [commit-msg]

ci:
  autofix_commit_msg: |
    chore: auto-fix pre-commit hooks

    [pre-commit.ci] auto fixes from pre-commit.com hooks
  autofix_prs: true
  autoupdate_branch: ""
  autoupdate_commit_msg: "chore: pre-commit autoupdate"
  autoupdate_schedule: weekly
  skip: []
  submodules: false
